FROM golang:1.26-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -ldflags="-s -w" -o /mcp ./cmd/mcp

FROM scratch
COPY --from=builder /mcp /mcp
ENTRYPOINT ["/mcp"]
