pre-commit:
  parallel: true
  commands:
    fmt:
      run: go fmt ./...
    vet:
      run: go vet ./...
    lint:
      run: golangci-lint run

pre-push:
  commands:
    test:
      run: go test ./...
